
Localmente:
$ sudo ifconfig enp0s3 down
$ sudo ifconfig enp0s3 10.19.25.36 netmask 255.255.255.0 up
$ sudo route add default gw 10.19.25.254

Ubuntu 16.04
$ sudo apt-get update
$ sudo apt-get -y install apache2
$ sudo apt-get -y install php
$ sudo apt-get -y install php-curl
$ sudo apt-get -y install libapache2-mod-php
$ sudo apt-get -y install php-pear php-dev
$ sudo pecl install mongodb

$ sudo vim /etc/php/7.0/apache2/php.ini

$ sudo a2dismod status
$ sudo a2enmod rewrite
Editar:
/etc/apache2/apache2.conf
Remover o comentário "#" da linha:
AccessFileName .htaccess


Mongo: Ubuntu 16.04

$ echo "deb http://repo.mongodb.org/apt/ubuntu xenial/mongodb-org/3.2 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-3.2.list
$ sudo apt-get update
$ sudo apt-get install -y mongodb-org

Follow this step ONLY if you are running Ubuntu 16.04.
Create a new file at /lib/systemd/system/mongod.service with the following contents:
------
[Unit]
Description=High-performance, schema-free document-oriented database
After=network.target
Documentation=https://docs.mongodb.org/manual

[Service]
User=mongodb
Group=mongodb
ExecStart=/usr/bin/mongod --quiet --config /etc/mongod.conf

[Install]
WantedBy=multi-user.target

-----

$ sudo service mongod start
$ sudo pecl install mongodb

Criando o db no mongo
$ mongo
$ use hackaflag
$ db.users.insertOne( { user:"admin", pass: "bfe7dc0d4bf2f0b83ad0a9e04f0bf0da" })


Instalando o composer:
$ php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
$ php -r "if (hash_file('SHA384', 'composer-setup.php') === 'aa96f26c2b67226a324c27919f1eb05f21c248b987e6195cad9690d5c1ff713d53020a02ac8c217dbf90a7eacc9d141d') { echo 'Installer verified'; } else { echo 'Installer corrupt'; unlink('composer-setup.php'); } echo PHP_EOL;"
$ php composer-setup.php
$ php -r "unlink('composer-setup.php');"
$ sudo mv composer.phar /bin/composer
$ sudo vim /etc/php/7.0/cli/php.ini
     Inserir: extension=mongodb.so

$ cd /var/www
$ git clone https://github.com/manoelt/roadsec-web-1
$ composer install
$ chmod 777 uploads
ADICIONAR HTACCESS no uploads
ALTERAR DOCUMENT ROOT para /var/www/roadsec-web-1/web
ALTERAR O CODIGO DE ACORDO COM O DOC ROOT
$ sudo apt-get install phantomjs

Durante o CTF executar o bot:
$ phantomjs bot.js


Criar arquivo de flag:
$ echo -n "DESEC{d3fc0n_ai_v0u_3u_ch4ll_h4rd} | /flag
Deixar apenas como leitura
